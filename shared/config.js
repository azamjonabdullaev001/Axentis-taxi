/**
 * ─────────────────────────────────────────────────────────────────────────────
 *  ОБЩИЙ КОНФИГ — единственное место для смены адреса бэкенда
 *  Меняй только BACKEND_HOST — всё остальное подтянется автоматически.
 * ─────────────────────────────────────────────────────────────────────────────
 */

const BACKEND_HOST = '84.247.138.53:8181';   // ← меняй здесь

const API_BASE = `http://${BACKEND_HOST}/api/v1`;
const WS_BASE  = `ws://${BACKEND_HOST}/ws`;

export { BACKEND_HOST, API_BASE, WS_BASE };
